// Site-wide constants.

export const BOOKING_URL = "https://cal.com/jack-loosbrock-wzgbta/meeting-callvia";

// Absolute origin. Open Graph and Twitter image URLs must be absolute, so
// without this Next resolves them against http://localhost:3000 and every
// shared link renders with a broken preview image.
export const SITE_URL = "https://callvia.io";
